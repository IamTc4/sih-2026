# ============================================================
# Merged requirements for all 4 Python backend services
# Blockchain + Agentic AI + ML Risk + Cybersecurity
# ============================================================

# ── Web Framework (all services) ────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0

# ── Blockchain Forensics ─────────────────────────────────────
networkx>=3.0

# ── Agentic AI (LangGraph) ───────────────────────────────────
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-google-genai>=2.0.0

# ── ML Risk Scoring ─────────────────────────────────────────
pandas>=2.2.0
numpy>=1.26.0
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.4.0

# ── Cybersecurity ────────────────────────────────────────────
apscheduler>=3.10.0

# ── Testing (needed for imports in some services) ───────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
